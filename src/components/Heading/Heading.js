import "./Header.scss";

const Heading = (props) => (
	<header className="wrapper-header">
		<h1 className="wrapper-header__title">TaskToDo</h1>
		<p className="wrapper-header__text">take control of your tasks</p>
	</header>
);

export default Heading;
